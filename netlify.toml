[build]
  base = "fitness-saas"
  publish = "."
  functions = "api"
  command = "npm install"

[build.environment]
  NODE_VERSION = "24"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
  force = true
