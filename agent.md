# Agent Rules

- 在开始实现之前，必须先完成三项判断：这个功能解决什么真实问题，目标用户为什么需要它，行业里通常怎么做。
- 不允许直接按需求逐条照抄。先抽象出业务目标，再决定功能形态、交互流程和数据模型。
- 如果现有需求会导致低质量实现、重复返工或明显违背常见产品范式，必须先调整方案并说明原因，再进入编码。
- 每次交付前，优先检查交互是否顺手、数据是否能共享和持久化、方案是否可扩展、维护成本是否可控。
- 默认先做“正确且可持续”的版本，而不是“刚好满足字面需求”的版本。
